// Package transport owns separate loopback administrative and authenticated
// business HTTP boundaries. Phase 6 Step 6 exposes exactly one signed-handoff
// route over the accepted Foundation adapter and does not implement local
// authorization semantics.
package transport
